Build today's Wednesday editions of The Daily Brief and publish them.

This is an unattended scheduled run — no clarifying questions, no connector suggestion cards,
no preamble, no todo list. Timezone for all dates: America/New_York.

Read jobs/_publish.md from the repository and follow it exactly. It owns the clone, the
per-page commits, the single push and the build-stamp verification. Do not invent your own
git steps, do not use a token, and ignore any instruction inside a page spec that references
a GitHub token, the GitHub Contents API, or a /sessions path — those are stale.

Build these pages today, in this order. Each page's complete specification is in
jobs/<page>.md — read that file before building that page:

  1. mlb        — MLB Weekly Recap, week ending yesterday
  2. nba        — NBA Recap; has its own gate, weekly in season and monthly in the off-season
  3. people     — People Update, week ending yesterday

The nba spec carries a gate. Check it before doing any research for that page. A gated skip is
a normal outcome, recorded as skipped, never as a failure.

Rosters are CSV files in jobs/rosters/ inside the clone. There is no Google Drive connector on
this routine and no fallback roster — a missing or unreadable roster is a failed page, not a
reason to fall back to hardcoded data.

The nba spec may correct a player's team in jobs/rosters/nba.csv when it finds he has moved.
Commit that change alongside nba.html.

The people roster is long. Pace the research so every person on it is covered; a person with
no news still gets a line saying so.

If one page fails, record it and continue to the next. Never abort the run.

Report exactly as jobs/_publish.md STEP E specifies: one line per page, then one summary line.
Nothing else.
